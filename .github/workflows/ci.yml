# CI/CD disabled during MVP development
# Re-enable after adding FLY_API_TOKEN to GitHub repository secrets
# and configuring linting properly

name: Raven CI/CD

on:
  workflow_dispatch:  # manual trigger only — not automatic

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI disabled during MVP. Deploy manually via fly deploy."
